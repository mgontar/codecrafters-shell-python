#!/bin/sh

[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
gnureadline = { version = "*", markers = "sys_platform == 'darwin'" }

[dev-packages]

[requires]
python_version = "3"
